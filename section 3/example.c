#include <stdio.h>
#include <stdlib.h>

int main()
{
    int boys = 15;
    int girls = 12;
    int students = boys + girls;

    printf("There are %d students in the class!", students);

    return EXIT_SUCCESS;
}